/** Third column: the agent-session strip lands here in M5. */
export function StripRegion() {
  return (
    <div className="flex min-h-0 items-center justify-center border-r border-border bg-sidebar/50">
      <span className="text-xs text-muted-foreground">agent sessions</span>
    </div>
  );
}
