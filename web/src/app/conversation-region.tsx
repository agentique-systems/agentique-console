/** Center stage: the operator↔orchestrator conversation lands here in M4. */
export function ConversationRegion() {
  return (
    <div className="flex min-h-0 items-center justify-center border-r border-border">
      <span className="text-xs text-muted-foreground">conversation</span>
    </div>
  );
}
